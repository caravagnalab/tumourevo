nextflow_process {
    name "Test Process CNA_PROCESSING (cna2CNAqc)"
    script "modules/local/cna2CNAqc/main.nf"
    process "CNA_PROCESSING"

    test("Should run without failures") {
        when {
            params {
                // define module specific parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                meta = [
                    id : "MSeq_Set06_Set6_42",
                    dataset : "MSeq",
                    patient : "Set06",
                    tumour_sample: "Set6_42",
                    cancer_type: "PANCANCER" ,
                    cna_caller : "sequenza"
                ]
                cna_segs = file(params.local_testdata_base_path + '/from_input/Set6_42.smoothedSegs.txt', checkIfExists: true)
                cna_extra = file(params.local_testdata_base_path + '/from_input/Set6_42_confints_CP.txt', checkIfExists: true)
                input[0] = tuple(meta, cna_segs, cna_extra)
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
