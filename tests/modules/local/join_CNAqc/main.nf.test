nextflow_process {
    name "Test Process JOIN_CNAQC"
    script "modules/local/join_CNAqc/main.nf"
    process "JOIN_CNAQC"

    test("Should run without failures") {
        when {
            params {
                // define module specific parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                meta = [
                    id : "MSeq_Set06_Set6_42",
                    dataset : "MSeq",
                    patient : "Set06",
                    tumour_sample: "Set6_42",
                    normal_sample: "54_N",
                    cancer_type: "PANCANCER"
                ]
                rds_list = file(params.local_testdata_base_path + "/from_CNAqc/MSeq_Set06_Set6_42_qc.rds", checkIfExists: true)
                input[0] = tuple(meta, rds_list, meta.tumor_sample)
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
