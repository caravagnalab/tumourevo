nextflow_process {

    name "Test Process GET_POSITIONS_ALL"
    script "modules/local/get_positions/main.nf"
    process "GET_POSITIONS_ALL"

    test("Should run without failures") {

        when {
            params {
                // define module specific parameters here
                // outdir = "tests/results"
            }
            process {
                """
                meta = [
                    id : "MSeq_Set06_Set6_42",
                    dataset : "MSeq",
                    patient : "Set06",
                    tumour_sample: "Set6_42",
                    normal_sample: "54_N",
                    cancer_type: "PANCANCER" 
                ]
                rds_list = file(params.local_testdata_base_path + "/from_vcf2CNAqc/MSeq_Set06_Set6_42_snv.rds", checkIfExists: true)
                sample_list = "Set6_42"
                input[0] = tuple(meta, rds_list)
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
