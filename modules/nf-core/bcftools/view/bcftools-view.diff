Changes in component 'nf-core/bcftools/view'
'modules/nf-core/bcftools/view/environment.yml' is unchanged
'modules/nf-core/bcftools/view/meta.yml' is unchanged
Changes in 'bcftools/view/main.nf':
--- modules/nf-core/bcftools/view/main.nf
+++ modules/nf-core/bcftools/view/main.nf
@@ -9,9 +9,6 @@
 
     input:
     tuple val(meta), path(vcf), path(index)
-    path(regions)
-    path(targets)
-    path(samples)
 
     output:
     tuple val(meta), path("*.{vcf,vcf.gz,bcf,bcf.gz}"), emit: vcf
@@ -25,9 +22,9 @@
     script:
     def args = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "${meta.id}"
-    def regions_file  = regions ? "--regions-file ${regions}" : ""
-    def targets_file = targets ? "--targets-file ${targets}" : ""
-    def samples_file =  samples ? "--samples-file ${samples}" : ""
+    // def regions_file  = regions ? "--regions-file ${regions}" : ""
+    // def targets_file = targets ? "--targets-file ${targets}" : ""
+    // def samples_file =  samples ? "--samples-file ${samples}" : ""
     def extension = args.contains("--output-type b") || args.contains("-Ob") ? "bcf.gz" :
                     args.contains("--output-type u") || args.contains("-Ou") ? "bcf" :
                     args.contains("--output-type z") || args.contains("-Oz") ? "vcf.gz" :
@@ -36,9 +33,6 @@
     """
     bcftools view \\
         --output ${prefix}.${extension} \\
-        ${regions_file} \\
-        ${targets_file} \\
-        ${samples_file} \\
         $args \\
         --threads $task.cpus \\
         ${vcf}
@@ -73,3 +67,7 @@
     END_VERSIONS
     """
 }
+
+        // ${regions_file} \\
+        // ${targets_file} \\
+        // ${samples_file} \\

'modules/nf-core/bcftools/view/tests/main.nf.test.snap' is unchanged
'modules/nf-core/bcftools/view/tests/vcf_gz_index.config' is unchanged
'modules/nf-core/bcftools/view/tests/vcf_gz_index_tbi.config' is unchanged
'modules/nf-core/bcftools/view/tests/nextflow.config' is unchanged
'modules/nf-core/bcftools/view/tests/main.nf.test' is unchanged
'modules/nf-core/bcftools/view/tests/vcf_gz_index_csi.config' is unchanged
************************************************************
