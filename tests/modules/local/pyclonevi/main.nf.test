nextflow_process {
    name "Test Process PYCLONEVI"
    script "modules/local/pyclonevi/main.nf"
    process "PYCLONEVI"

    setup {
        run("RDS_PROCESSING") {
            script "../../../../modules/local/CNAqc2tsv/main.nf"
            process {
                """
                meta = [
                    id : "MSeq_Set06_Set6_42",
                    dataset : "MSeq",
                    patient : "Set06",
                    tumour_sample: "Set6_42",
                    normal_sample: "54_N",
                    cancer_type: "PANCANCER" 
                ]
                joinCNAqc_rds = file(params.local_testdata_base_path + "/from_joinCNAqc/MSeq_Set06_multi_cnaqc.rds", checkIfExists: true)
                tumor_samples = "Set6_42"
                input[0] = tuple(meta, joinCNAqc_rds, tumor_samples)
                """
            }
        }
    }

    test("Should run without failures") {
        when {
            params {
                // define module specific parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = RDS_PROCESSING.out
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
